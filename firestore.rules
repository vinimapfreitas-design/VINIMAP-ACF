rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para coleções operacionais da frota ViniMap
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
